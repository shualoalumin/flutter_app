#include "AppInfo.xcconfig"

// The prefix for the debug build's bundle identifier
PRODUCT_BUNDLE_IDENTIFIER_PREFIX = com.example

// The code signing identity to use for debug builds
CODE_SIGN_IDENTITY = "Apple Development"
